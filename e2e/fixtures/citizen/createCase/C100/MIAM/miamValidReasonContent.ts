export enum MiamValidReasonContent {
  pageTitle = "Do you have a valid reason for not attending a MIAM?",
  govukBodyL = "Check the list of valid reasons for not attending a MIAM (opens in a new tab) if you’re not sure.",
  govukLabel1 = "Yes",
  govukLabel2 = "No",
  errorMessage = "Select yes if you have a valid reason for not attending a MIAM",
}
