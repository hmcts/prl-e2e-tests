export enum PassportOfficeContent {
  pageTitle = 'Do any of the children have a passport?',
  caption = 'Safety concerns',
  errorSummaryList = 'Select yes if any of the children have a passport',
  errorMessage = 'Select yes if any of the children have a passport',
}